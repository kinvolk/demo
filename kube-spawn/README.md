# Demo: two parallel kube-spawn clusters

This demo starts two kube-spawn clusters: a two-node Docker cluster, and a three-node rktlet cluster.

## Dependencies

* asciinema

## Run the demo

```
$ asciinema play -s 4 twoclusters.json
```
